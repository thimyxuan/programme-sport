<ul class="">
    
    <li>Mon objectif >></li>
    {% for objectif in objectifs %}        
        <li>
            <a href="{{ path('objectif', {id: objectif.id}) }}">{{ objectif.titre }}</a>
        </li>
    {% endfor %}
</ul>
